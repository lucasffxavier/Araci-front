<UserControl x:Class="Araci.Properties.CaboPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#252526">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->

        <Border Background="#2D2D30"
                BorderBrush="#3E3E42"
                BorderThickness="0,0,0,1"
                Padding="12">

            <TextBlock Text="PROPRIEDADES DO CABO"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="White"/>
        </Border>

        <!-- CONTEÚDO -->

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">

            <Grid Margin="12">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- NOME -->

                <TextBlock Text="Nome"
                           Foreground="#CCCCCC"
                           Margin="0,0,10,12"/>

                <TextBox Grid.Column="1"
                         Margin="0,0,0,12"
                         Text="{Binding Nome, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- BARRA 1 -->

                <TextBlock Grid.Row="1"
                           Text="Barra 1"
                           Foreground="#CCCCCC"
                           Margin="0,0,10,12"/>

                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Margin="0,0,0,12"
                         Text="{Binding Barra1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- BARRA 2 -->

                <TextBlock Grid.Row="2"
                           Text="Barra 2"
                           Foreground="#CCCCCC"
                           Margin="0,0,10,12"/>

                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Margin="0,0,0,12"
                         Text="{Binding Barra2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- COMPRIMENTO -->

                <TextBlock Grid.Row="3"
                           Text="Comprimento"
                           Foreground="#CCCCCC"
                           Margin="0,0,10,12"/>

                <TextBox Grid.Row="3"
                         Grid.Column="1"
                         Margin="0,0,0,12"
                         Text="{Binding Comprimento, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- LINECODE -->

                <TextBlock Grid.Row="4"
                           Text="LineCode"
                           Foreground="#CCCCCC"
                           Margin="0,0,10,12"/>

                <TextBox Grid.Row="4"
                         Grid.Column="1"
                         Margin="0,0,0,12"
                         Text="{Binding LineCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            </Grid>

        </ScrollViewer>

    </Grid>

</UserControl>