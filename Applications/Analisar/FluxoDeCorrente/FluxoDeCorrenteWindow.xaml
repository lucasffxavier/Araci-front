<Window x:Class="Araci.Applications.Analisar.FluxoDeCorrente.FluxoDeCorrenteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fluxo de Corrente"
        Height="230"
        Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="2"
                   Text="Pasta para salvar o arquivo DSS:"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6"/>

        <TextBox x:Name="PastaTextBox"
                 Grid.Row="1"
                 IsReadOnly="True"
                 VerticalContentAlignment="Center"
                 Margin="0,0,8,0"/>

        <Button Grid.Row="1"
                Grid.Column="1"
                Content="Selecionar Pasta"
                Click="SelecionarPastaButton_Click"/>

        <TextBlock Grid.Row="3"
                   Grid.ColumnSpan="2"
                   Text="Nome do arquivo:"
                   FontWeight="SemiBold"
                   Margin="0,0,0,6"/>

        <TextBox x:Name="NomeArquivoTextBox"
                 Grid.Row="4"
                 Grid.ColumnSpan="2"
                 VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="6"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="OK"
                    Width="90"
                    IsDefault="True"
                    Margin="0,0,8,0"
                    Click="OkButton_Click"/>

            <Button Content="Cancelar"
                    Width="90"
                    IsCancel="True"
                    Click="CancelarButton_Click"/>

        </StackPanel>
    </Grid>
</Window>
